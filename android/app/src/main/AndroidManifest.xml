<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network access. Previously declared only in the debug/profile variant
         manifests, so release builds shipped with no internet at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Android 13+ runtime notification permission (FCM + local notifications). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Scheduled local notifications survive reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Task reminders fire at a time the user chose, so they need an exact
         alarm. Android 12 grants this outright; 13+ lets the user revoke it and
         14+ denies it to most apps by default, so the app checks
         canScheduleExactNotifications() and falls back to an inexact alarm
         rather than silently scheduling nothing.

         Deliberately NOT USE_EXACT_ALARM: that is reserved for alarm-clock and
         calendar apps, and Play rejects apps that declare it without
         qualifying. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <application
        android:label="SyncUp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Custom-scheme deep links. Work without domain verification, so
                 group invites and emailed auth links function immediately:
                   syncup://groups/join/<token>
                   syncup://auth/verify-email?token=<token>
                   syncup://auth/reset-password?token=<token> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="syncup" android:host="groups"/>
                <data android:scheme="syncup" android:host="auth"/>
            </intent-filter>

            <!-- Verified App Links for the same routes over https. Requires
                 assetlinks.json at https://syncup.app/.well-known/assetlinks.json
                 before autoVerify succeeds; until then Android shows a chooser. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="syncup.app"
                      android:pathPrefix="/groups/join"/>
                <data android:scheme="https" android:host="syncup.app"
                      android:pathPrefix="/auth/verify-email"/>
                <data android:scheme="https" android:host="syncup.app"
                      android:pathPrefix="/auth/reset-password"/>
            </intent-filter>
        </activity>
        <!-- Presentation for pushes that FCM draws itself, i.e. every one that
             arrives while the app is backgrounded or killed. Without these the
             system uses the launcher icon (a white square once silhouetted) and
             files the notification under a "Miscellaneous" channel separate
             from the one foreground pushes use, so the two look unrelated and
             cannot be muted together. The channel id must match the channel
             NotificationService creates at startup. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_syncup" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="syncup_default" />

        <!-- flutter_local_notifications ships these receivers as Java classes
             but does NOT declare them in its own manifest, so a host app must.
             Without them a scheduled alarm fires into an intent that resolves
             to nothing and no notification ever appears — not just after a
             reboot, but ever. Nothing logs a warning; it simply never arrives.

             ScheduledNotificationBootReceiver re-registers pending alarms after
             a restart or an app update, which is what RECEIVE_BOOT_COMPLETED
             above is for. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
